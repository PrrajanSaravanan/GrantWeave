import React from 'react';
import GrantWrapped from '../components/GrantWrapped';

const WrappedPage = () => {
    return (
        <div className="h-full overflow-y-auto w-full p-6">
            <GrantWrapped />
        </div>
    );
};

export default WrappedPage;
